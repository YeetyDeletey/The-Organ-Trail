x = 680 - (global.distancetolandmark * 10)
image_xscale = 4.5
image_yscale = 4.5

/*
I wasted a bunch of time on this before deciding not to include it
the idea was that another trailmarker would be there if positionwise
there should be 2 on screen, but one would appear based on which split
path you take, and it would be inconsistent to include/not include it 
as a result so I'm cutting it.
if (!first) {
	//sprite_index = whatever the fort sprite ends up being
}